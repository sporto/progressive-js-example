import Marty      from 'marty';

export default Marty.createConstants([
	// 'CHANGE_KEYWORD',
	// 'ON_KEYWORD_CHANGED',
	'ON_LIBRARIES_FETCHED', 
	'ON_LIBRARIES_FETCHED_FAILED', 
	// 'ADD_LIBRARIES'
	]);